# Project Notes

This is a Markdown file. It carries no symbols — the indexer should record it
as a `file` row (searchable, diff-trackable) but extract zero symbols.

## Architecture

The service is split into a Python backend and a JavaScript frontend.
