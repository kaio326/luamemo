# Local eval artefacts and downloaded datasets
eval/data/
eval/results/
eval/*.json
*.log
*.swp
*~
.DS_Store
*.src.rock
*.rock

# Dev container environment file (may contain MEMO_MASTER_KEY)
dev/.env

# Local secrets — never commit
.github/
secrets/
research/
.env
.env.backup

# Workspace-local MCP config written by memo calibrate
.vscode/mcp.json
