# Local eval artefacts and downloaded datasets
eval/data/
eval/results/
eval/*.json
eval/tmp/
*.log
*.swp
*~
.DS_Store
*.src.rock
*.rock

# Dev container environment file (may contain MEMO_MASTER_KEY)
dev/.env

# Local secrets — never commit
.github/
secrets/
research/
.env
.env.backup

# Workspace-local MCP config written by memo calibrate
.vscode/mcp.json

# Per-project local config written by memo calibrate (embedder/DB selection)
.luamemorc

# Phase 7 native embedder — built locally, never committed
luamemo/embedders/native/*.so
*.gguf
